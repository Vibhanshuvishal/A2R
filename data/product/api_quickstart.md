# API quickstart

Create an API token from the developer settings and keep it secret. Send the token in the documented authorization header for every request. Start with the account endpoint to verify authentication. Requests are subject to rate limits; clients should handle rate-limit responses with backoff. Use a test workspace before sending production data.
