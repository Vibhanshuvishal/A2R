# Data export

Administrators can export workspace records as CSV from the export page or retrieve approved data through the API. Exports are prepared asynchronously and are available to the requesting administrator. Retention and deletion follow the workspace retention setting. Large exports may be split into several files for reliable download.
