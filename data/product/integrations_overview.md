# Integrations overview

The integrations catalog includes Slack, Salesforce, Jira, and Zapier connectors. An administrator authorizes an integration and chooses which workspace data it may access. Disconnecting an integration stops future synchronization but does not automatically delete records already created in the external service. Review each integration's permissions before enabling it.
